#include <bits/stdc++.h>
using namespace std;
struct stu{
	string name;
	int Chinese;
	int English;
	int math;
}a,ans;
int main(){
	int n;	
	cin >> n;
	ans.Chinese = -1; ans.math = -1; ans.English = -1;
	for(int i=1;i<=n,i++){
		cin >> a.name >> a.Chinese >> a.math >> a.English;
		if(a.Chinese + a.math + a.English){
			
		}
	} 
}
